# THE HANDOFF DOCUMENT
**yt-shorts-bot / sale91.com — build plan, 2026-09-04**
Repo: `/Users/ankit/Projects/yt-shorts-bot` · also `/Users/ankit/Projects/catalog`

---

## 30-SECOND SUMMARY

1. **Nothing is measurable.** `grep utm_ daily_short.py` returns **0**. 267 videos, 138 blog posts, and not one tagged link — every claim below is currently unfalsifiable. Fix this first; it is three string edits.
2. **The site's own root `/llms.txt` undercuts his rate card by ₹5–22/pc on all 17 products** and still contains the fabricated "₹40K–15L loss" language the blog was cleaned of. The correct file already exists at `/catalog/llms.txt` (26,752 b); the root copy (16,939 b) was never deployed. Deploy bug, not strategy.
3. **The fabrication fix was applied to 1 of 3 title branches and 0 of 1 cover generators**, and `review_script()`'s HOOK rubric still *rewards* the exact phrase the writer is forbidden to use — so the retry loop actively pushes the bot back toward inventing losses.
4. **The Short never asks anyone to visit sale91.com** until 80% in, and never out loud. The prompt forbids it in three places; the only always-on brand mark is a 24 px badge at 70% opacity.
5. **His own data says honesty costs nothing**: titles containing an explicit GSM number median 3,228 views / 0.89% share vs account median 2,302 / 0.43%. The two highest-share reels ever contain no invented loss.
6. **Cost is not a problem** — $136/mo total, 87.5% Veo, true Claude spend ~$6.80/mo. Do not switch providers. Do fix `COST_RATES`, which is 2.4× wrong and feeds the $10 circuit breaker.

---

## PART A — ALREADY EXISTS. DO NOT REBUILD.

Every line below was read, not inferred. If a recommendation in a future session duplicates something here, it is a failure.

### The daily Short pipeline is complete and sophisticated
| Thing | Where | Note |
|---|---|---|
| Trigger | `.github/workflows/daily_short.yml:10` | `0 9 * * 1-6` = 14:30 IST Mon–Sat; Sunday → `sunday_recap.yml` |
| Topic scoring gate before spend | `review_topic()` `daily_short.py:7532` | /40, auto-zero for invented incidents at `:7569-7573` |
| Script retry loop w/ feedback re-injection | `main()` `:12075-12157`, feedback at `:12081` | `SCRIPT_MAX_ATTEMPTS=5` |
| ElevenLabs PVC voice clone | `:145` / `:148` / `:151`; fallback Sarvam `:6998` → OpenAI `:12230-12301` | Post = atempo 0.95 + loudnorm −14 LUFS only (`:12309-12336`). **No denoise. Correct.** |
| 5× Veo 3.1 Fast clips, freshly generated | `:184-209`, loop `:12464-12599`; Kling fallback `:11878` | Not stock, not text cards |
| Ken Burns, black-intro trim, crossfade, loop-guard | `:12880`, `:12915`, `:12998-13005`, `:12975-12996` | |
| Music + ducking + bass drop + whooshes | `:5706`, `:7097`, `:7132`, `:7229`, `:7257` | |
| Karaoke captions, Whisper force-aligned | `_forced_align_caption_times()` `:6635`, render `:12650-12830`, y=0.50 `:13110` | **LIVE** — `daily_short.yml:170` sets `ADD_SUBTITLES=1` |
| Hook in first 3s, three layers | panel `:13158-13215`, SFX `:7132`, clip-1 mid-action mandate `:5312`/`:5347` | |
| Thumbnail: research → brief → Gemini text-free scene → own PIL text | `:1045` → `:1203` → `:1421` → `compose_cover_text()` `:847` | CVD-safe yellow `#FFD400` at `:367`, safe band 0.26–0.70 at `:868` |
| 3 platform-tuned titles | `optimize_title()` `:7795` (yt/ig/blog) | |
| Publish chain | YT `:5596` (private + `publishAt`) → pin `:1532` → playlist `:1670` → IG `:2181` → FB `:1987` → Telegram `:2073` → blog `:8842`/`:11334` | |
| **Measured winner format (v2, on his own 74 reels)** | `:1269-1300` | 2 lines split by `\|`, 3–6 words TOTAL; named-pain 0.53% share vs generic curiosity 0.23%; statements out-share questions 2.4× |
| **Safe band 25–70% verified** | `:1317-1318` | Survives YT Shorts UI *and* IG 4:5 grid crop |

### The fabrication guardrails that DO exist
- `_fabricated_incident_sentences()` `:11281` — working deterministic detector, already false-positive-tuned against all 138 live posts. Wired at `:11344` (blog) and `:3126` (IG carousel). **The logic is done. It needs a third call site, not a rewrite.**
- IG carousel gate `:3126-3155` — amount+lossword pair check, blocks 11 marketplace/courier names, fails safe.
- Script prompt ban `:5001-5028`, `:5074-5076`; topic prompt ban `:7427-7457`; `review_topic()` auto-zero `:7569-7573`. Four commits (`075c4bb`, `88ae824`, `f0a942b`, `3360fe4`, 24–27 Aug).

### AI-search / structured data — already strong
- **Full JSON-LD on every blog post**, `inject_blog_seo()` `:7988-8287`: Organization, Person `@id #ketu-r`, BreadcrumbList, Article, Speakable, Product+AggregateOffer, VideoObject, HowTo, FAQPage. Live-verified: polo post serves **7** `ld+json` blocks. Fake `aggregateRating` already removed, documented as a landmine at `:8120-8122`.
- **llms.txt estate — 4 files, 2 extra languages.** Dynamic `/p/llms.txt` `:10673-10751` (35 KB, 125 articles) + `/p/llms-full.txt` `:10754-10864` (52 KB). Static via `/Users/ankit/Projects/catalog/scripts/sync-llms.js`: `/catalog/llms.txt` (26,752 b), `llms-full.txt` (41,173 b), `llms-hi.txt`, `llms-ta.txt`, `.well-known/llms.txt`.
- **AI-crawler allowlist shipped** `:11731-11800` — GPTBot, ChatGPT-User, Google-Extended, Applebot-Extended, anthropic-ai, ClaudeBot, PerplexityBot, Bytespider, cohere-ai, YouBot, Meta-ExternalAgent, Bingbot.
- **Push-indexing wired** — Google Indexing API `:11628`, IndexNow → api.indexnow.org / bing / yandex `:11647-11684`, key file auto-upload `:11710-11728`.
- **Sitemap chain healthy** — root is a proper sitemapindex (200) → `/p/map.xml` (125 URLs) + `/catalog/sitemap.xml` (24 URLs). July's "403" and "not declared" items are FIXED.
- **Per-GSM catalog pages are excellent** — `/catalog/p/oversize-240gsm/` = 2,097 words, 3 tables, 15-question FAQPage, Product LD with **two** AggregateOffers carrying `eligibleQuantity` (bulk ₹195 @ 10+, sample ₹233 @ 1–9), 27 images.
- **`products.json` exists and is byte-correct** — 37 KB, all 17 products diffed against live `pc.js`: **exact match**. Carries `bulkPriceFrom/To`, `pricePerSize`, `priceBands`, `catalogUrl`, `gstRate:5`, `moq:10`.
- **Homepage has real LocalBusiness schema** — `@id #business`, F120 Khanpur New Delhi 110062, geo 28.511791/77.230149, hours, languages.
- **Canonical discipline correct** — sale91.com and www.bulkplaintshirt.com serve byte-identical HTML (md5 match), both canonical to www.bulkplaintshirt.com.
- **Reddit backlinks: settled, closed.** `memory/project_yt_shorts_bot_external_seo.md` (01-Aug correction) — all nofollow, API posting banned, 5 of 7 target subs forbid it. Do not re-raise.

### IG measurement loop — exists and is good
- `check_instagram_engagement()` `:3486-3692`, invoked `:13373` **before** the YouTube auth gate deliberately (`:13365-13367`). Fetches `views,reach,saved,shares` `:3622` with `plays` fallback `:3628`, likes/comments `:3586`, `ig_reels_avg_watch_time` in an isolated call `:3659` so an unsupported metric can't break the primary fetch. 48h delay, 96h for trial reels `:3524-3527`. Resolves `scheduled:<container_id>` → real media id `:3541-3578`. Retires dead media after 3 strikes `:3596-3602`.
- **He already proved shares beat likes on his own account** — comment at `:3709-3711`: *Spearman 0.45 vs likes 0.13 on 98 reels.*
- Summary is wired into thumbnail research `:1083-1093`, brief `:1245-1251`, topic pick `:1444`, script gen `:7819`.
- `tools/crosspost_watch.py:147-166` already enumerates `/{ig-biz}/media` with the right fields. `crosspost_ledger.json` + `crosspost_state.json` already model expected-vs-actual across IG/FB/X/LinkedIn/YouTube; `.github/workflows/crosspost_watch.yml` runs 2×/day.
- `.github/workflows/fs_insights.yml:38` already probes the richer metric set with graceful HTTP-400 fallback — the correct pattern, currently inert.

### Filmed-reel toolchain — complete, battle-tested, disconnected
`tools/reel_pipeline/`: `build_reel.py`, `cap2.py`, `cover.py`, `prepend_cover.py`, `audio_ab.py`, `mkreel2.py`, `lines_example.py`, `README.md`, `v18_social_v2.json`.
**`cap2.py:36` `HI = (127,212,255)` = #7FD4FF blue, with the CVD reason in the docstring at `cap2.py:15-16`.** 0.5 s cover baked in-filtergraph. Audio = highpass 60 + loudnorm, never denoise.

### Cost infrastructure
`CostTracker` `:2614-2743` — per-service USD, JSON history, breakdown print `:2692`, real circuit breaker `DAILY_COST_LIMIT_USD=10.0` at `:420` enforced at `:12041`. Model tiering already exists and is roughly right (Opus for script+judges, Sonnet for the 16k blog, Haiku for IG carousel + Reddit). Health probe deliberately uses a 1-token *message* call, not `/v1/models`, so an empty credit balance actually surfaces (`health_watch.py:209-213`).

### Standing rules already encoded elsewhere — do not "discover" these again
- **AWB / label / courier bag / barcode in any parcel frame: BANNED.** `project_factory_sach_script_pack.md:35-38`.
- **Burn test is his measured WORST format** — 11,746 views @ 0.75% shares vs 56,767 @ 2.70% for the zero-prop reel. `project_factory_sach_script_pack.md:19-21`.
- **Speaking rate 2.06 words/sec measured** — `:23-27` of the same file.
- **Allowed godam prop inventory is a fixed list** — `:17-22`.
- **No ₹ figure may live in a prompt or memory.** Rates are read off live `pc.js` at shoot time. `project_dk2_wrong_nonanswer_audit.md:25`, `reference_pcjs_publish_and_restore.md:11`.

---

## PART B — GENUINELY MISSING

1. No UTM tagging anywhere. **0 hits** for `utm_`.
2. Root `/llms.txt` + `/llms-full.txt` never deployed; sitemap points AI crawlers at the stale copies.
3. `_fabricated_incident_sentences()` has no call site for `yt_title`, `hook_text`, `script_voice`, or cover text.
4. `review_script()` HOOK/ENDING rubric contradicts the writer prompt. No TRUTH dimension.
5. YouTube + blog title branches of `optimize_title()` have no truth constraint; the shared context block actively feeds disaster framing back as patterns to copy.
6. `generate_thumbnail_brief()` `:1203-1420` — **zero** fabrication guard anywhere in the range.
7. No shot-format library. `TOPIC_BANK` and `search_trending_topics()` generate subjects, never camera setups.
8. No `format` field on any engagement record — per-format share-rate is unobtainable retroactively.
9. The 11 filmed reels in `publish_queue/done/*.json` are 100% absent from `ig_engagement_history.json`. `publish_queue.yml` never calls `save_ig_upload_record()`.
10. Insights check is one-shot (`checked=True` at `:3668`, hard skip at `:3509`). Median measurement age **73h**.
11. No winner-selection, promotion, or staging code exists. `grep winner|promote|repost` → only unrelated SEO hits.
12. No OAuth write credential for the main channel. `daily_short.yml:106` loads one token (bot channel); main is read-only via `CHANNEL_ID_2` `:432`.
13. `containsSyntheticMedia: True` hardcoded at `:5651` — would falsely label filmed footage.
14. Nothing in the repo reads Search Console. Google's *Search generative AI* inclusion setting (worldwide since 2026-08-31) has never been checked.
15. Blog Product LD hardcodes `lowPrice "65"` / `highPrice "250"` at `:8114-8115` and `'180-220 GSM, own knitted from Tiruppur'` at `:8108`, on 125 indexed pages.
16. Zero `/catalog/p/*` deep links from the blog (0 of 6 sampled posts).
17. `products.json` is linked from nowhere — 0 references in catalog HTML, any llms.txt, or any sitemap.
18. No prompt caching (`grep cache_control` → nothing).
19. 7 of 12 Opus call sites never call `track_claude_call`.
20. In-video CTA bar is red-orange `(230,60,20)` at `:13226` — verified.

---

## PART C — RANKED BUILD QUEUE

Ranked by impact per unit effort. Items 1–5 are the honest priority order; do them in order.

---

### 1. UTM-tag every sale91.com emission
**Build:** append `?utm_source=…&utm_medium=…&utm_campaign={topic_slug}` to every site URL the pipeline emits.
**Files:** `daily_short.py:5612` (YT description), `:392` and `:394` (pin comment variants), `:2322` (IG caption), `:12183-12186` (blog backlink), `:13302` (outro card — display text stays clean, link doesn't apply), `catalog` product links added by item 9.
**Why it drives traffic:** it does not, directly. It makes every other item on this list falsifiable. Right now nobody can say whether 267 videos have sent one buyer to the site. Ties into the reflink/analytics tooling that already exists (`project_reflink_worker.md`).
**Effort:** small — five string edits plus one `_tagged(url, medium)` helper.
**Verify:** `grep -c utm_ daily_short.py` > 0; then after one publish, confirm the tagged URL appears in the YT description, the pinned comment, and the IG caption; check Cloudflare path analytics (`reference_cc1_cloudflare_path_analytics.md`) for the first tagged hit within 48h.

---

### 2. Ship the root `/llms.txt` and `/llms-full.txt`
**Build:** add a bucket-root sync step to `/Users/ankit/Projects/catalog/.github/workflows/deploy.yml` (it currently syncs only under the `/catalog/` prefix), covering `llms.txt`, `llms-full.txt`, `.well-known/llms.txt`. Then repoint `catalog/sitemap.xml`, which currently lists the **root (stale)** URLs while the fresh `/catalog/` copies are in no sitemap at all. Invalidate CloudFront; respect the 4h Cloudflare cache trap (`reference_cloudflare_4h_cache_deploy_trap.md`).
**Why it drives traffic:** ChatGPT/Perplexity/browser agents fetch the conventional root path. Right now that path teaches them Oversize 240gsm = ₹180 when he sells at ₹195 — every product ₹5–22/pc low (210gsm ₹175 vs ₹190 · 180gsm ₹165 vs ₹177 · Premium Polo ₹220 vs ₹237 · Zip Hoodie ₹325 vs ₹337 · 430 hoodie ₹418 vs ₹440 · AcidWash ₹220 vs ₹238). It also still carries the fabricated `₹40K-15L losses`, `₹65-95 for 180 GSM`, `₹140-220`, `₹1.2 Lakh Lost` lines. The local `/Users/ankit/Projects/catalog/llms-full.txt` is already clean — grep confirms zero hits for all of them.
**Effort:** small — a workflow step and a sitemap edit. The correct file already exists on disk.
**Verify:** `curl -sI https://www.bulkplaintshirt.com/llms.txt` returns **26,752** bytes, not 16,939; `curl -s .../llms-full.txt | grep -c "₹40K"` = 0; sitemap contains the canonical copy only.
**Caveat to state out loud:** Google Search ignores llms.txt and says it will neither help nor harm rankings. This is for non-Google engines and agents. It is worth doing only because the file already exists and is currently *wrong*.

---

### 3. Fix `review_script()` so the retry loop stops manufacturing lies
**Build:** three edits in `daily_short.py`.
- `:7745-7746` — replace the HOOK criterion. It currently reads *"Does it start with a STORY, customer incident…? Good: 'Ek customer aaya tha…'"* — the exact phrase `:5025` forbids the writer to use. Replace with: *"Opens in ≤10 words on a checkable spec, a real published rate, or a question buyers ask."*
- `:7754-7755` — replace the ENDING criterion (currently rewards *"trail off naturally… '…bas yehi hai, simple hai.'"*) with *"hard 3–6 word stop, no trail-off"*, matching `:5036-5045` and the deliberate no-fade at `:13246-13248`.
- Add a 7th dimension **TRUTH** that auto-rejects any asserted incident (`:7770` already auto-rejects any dim < 4).
Then call the existing `_fabricated_incident_sentences()` on `script_voice + hook_text` as a deterministic pre-check *before* the Claude review runs — free when clean.
**Why it drives traffic:** this is the root cause that re-introduces fabrication after it was "fixed". An honest spec-based hook currently gets flagged weakest=HOOK, the verdict is fed back at `:12081`, and the loop converges on a lie published under his real business name where buyers can quote it back at him.
**Effort:** small. `_fabricated_incident_sentences()` `:11281` is already written and already tuned.
**Verify:** run the pipeline with `ADD_SUBTITLES=0` and dry-run publish; confirm a spec-based hook ("240 GSM = 210 gram") scores ≥4 on HOOK; confirm a seeded fabrication ("Ek customer ne ₹50,000 ka order cancel kar diya") hard-fails before the Claude call.

---

### 4. Give the YouTube and blog title branches the guard the IG branch already has, then gate the output
**Build:**
- Delete the disaster-framing "patterns that engage" block at `:7830-7834` (*"Cost/disaster framing engages: 'Cost Trap', 'Shut Down', 'Cost Lakhs'"*, *"Third-person founder stories work: 'He Bought X & Shut Down'"*) and the identical no-data default at `:7838-7840`.
- Delete the `"Lost Rs 40K on Tri-blend Fabric…"` allowed-example at `:7879`.
- Copy the IG branch's truth clause verbatim from `:7867-7871` into the YouTube branch at `:7859-7863` (which currently asks for *"Personal angle / vulnerability or dramatic comparison"* + *"Specific numbers + dramatic framing"* with **no** truth constraint).
- In `main()` right after `:12168`, run `_fabricated_incident_sentences(yt_title + ". " + hook_text)` and regenerate on a hit — the same way the blog refuses to publish at `:11344`.
- **Sweep `generate_thumbnail_brief()` `:1203-1420`, which was never touched at all.** `:1272` still mandates *"A. LOSS: [micro-₹ ≤₹20 cause] + [object] + [failure/batch] → '₹12 Thread | 500 RETURN'"* on a rotation; `:1297` still calls `'₹6 Tag = RETURN'` one of "his real winners"; `:1279-1280` still requires a STAKE of crack/RETURN/bleed/destroyed plus a batch size. So an honest script can still be topped with an invented cover.
- Delete the four surviving examples the 27-Aug sweep walked past: `:5291` heading *"Good hook texts (number/loss driven…)"*, `:5292` `"₹40,000 KI GALTI"`, `:5293` `"500 PIECE BARBAAD"`, `:5099` `"Ek customer ne ₹50,000 ka order cancel kar diya"`, `:5152` `"aur wahi ₹40,000 wali galti"`. A model copies examples over prose — that is exactly how ~60 fake disasters shipped.
- Replace with honest equivalents his data supports: `240 GSM = X GRAM`, `AAJ KA RATE ₹X`, `SAME GSM | ALAG FEEL`, `180 GSM | LIGHT AAR-PAAR`.
- Also fix `:1315`, which recommends **"Red (#FF0000)"** as a cover text colour.
**Why it drives traffic:** the title is what a B2B buyer judges first, and it is the surface still publishing invented losses — verifiable in `ig_engagement_history.json` *after* the 24-Aug fix: 25-Aug *"Rs 8 ka polybag skip kiya — 600 tshirt mein fungus laga"*, 27-Aug *"Rs 18K ka maal sirf Lockstitch na hone se wapas aaya"*.
**Effort:** small (prose edits) + small (one gate call).
**Verify:** grep `daily_short.py` for `40,000`, `BARBAAD`, `RETURN`, `#FF0000` → 0 hits in prompt strings. Publish one run and confirm the title contains no ₹ figure that isn't in `pc.js`.

---

### 5. Fix the clip-dedup truncation
**Build:** in `_get_recent_clip_prompts()` `:4900-4913`:
```python
for p in entry.get("prompts", [])[:2]:      # ← keep all 5
    lines.append(f"  - {p[:80]}...")        # ← 80 chars is entirely inside the STYLE LOCK preamble
```
Strip the preamble before truncating (split on the first `. ` after `STYLE LOCK`), keep all 5 prompts, and feed the last 5 style locks separately with an instruction that today's must differ from all of them.
**Why it drives traffic:** every Veo clip is freshly generated and costs real money, yet the "DO NOT reuse similar scenes" list Claude sees at `:5343-5344` is ten near-identical lines of lighting boilerplate with zero scene content — verified against `clip_history.json`. Across the last 20 videos × 5 prompts: `Indian` ×174, `handheld` ×105, `white` ×104, `shallow` ×102, `hands` ×80, `table` ×57. Every day is hands-on-fabric-on-a-table. On a Reels grid — which is exactly where a scrolling buyer decides — the daily Short is indistinguishable from yesterday's.
**Effort:** small — one function, string slicing. `clip_history.json` already persists everything needed.
**Verify:** print the dedup block before the Claude call; it must contain scene nouns, not lighting adjectives. After 5 runs, recount the token histogram above — `hands`/`table` should drop materially.

---

### 6. Replace the dead outro card with a live-frame CTA, and allow exactly one spoken closing line
**Build:**
- Drop the `concatenate_videoclips` at `:13313`. Hold the last live frame under a 1.5 s bottom-third bar instead (the renderer at `:13218-13242` already exists — this is repositioning, not new code).
- Relax the three-place spoken-CTA ban (`:5090` rule 10, `:5153-5155` rule 16, `:5238` ending rule 5) to permit exactly one 4–6 word line after the loop-back: e.g. *"Rate list Sale91 pe hai."*
- Move the always-on badge out from under the hook panel: it sits at y=0.17 (`:273-274`) while the hook panel draws at y=0.18 (`:13167`, `:13195`).
**Why it drives traffic:** biggest single lever on the Short actually sending someone to sale91.com. The script prompt spends a whole rule (`:5150-5156`) building a loop-back ending to earn replays, then `:13313` appends a 2 s solid `(15,15,25)` card *after* it — the loop never closes, and ~6% of runtime is a frame nobody watches. Meanwhile for the first ~26 of ~32 seconds there is no legible reason to visit the site and never a spoken one, at exactly the moment a B2B viewer who just learned a real check is at peak trust.
**Effort:** medium — a prompt-rule change plus a composite reposition.
**Verify:** render one Short; confirm total duration drops by 2s, the final frame is live footage, the loop replays seamlessly, and the closing line is present in `script_voice` and audible. Check the UTM link from item 1 registers a click.

---

### 7. Switch the CTA bar to CVD-safe blue
**Build:** `daily_short.py:13226` — `ColorClip(..., color=(230, 60, 20))` → `(127, 212, 255)` (#7FD4FF).
**Why:** standing rule; he is red-green colour blind. The thumbnail path already respects it (`COVER_HL_YELLOW` at `:367` is annotated CVD-safe) and `cap2.py:36` already implements the blue with the reason in its docstring at `cap2.py:15-16`. The in-video CTA never got the treatment.
**Effort:** one tuple.
**Verify:** screenshot frame at `total_duration-2.0`; sample the bar pixel.
**Related, lower priority:** `build_reel.py:31 HI=(255,215,0)`, `cover.py:14 YELLOW=(255,214,10)`, `daily_short.py:251 SUBTITLE_HIGHLIGHT_COLOR="yellow"` are all yellow. Yellow is CVD-safe and *fine* — do not "fix" these. Only red/green as a meaning-carrying pair is forbidden.

---

### 8. Silence or sample-gate the own-channel performance loop
**Build:** in `_own_channel_performance_signal()` `:4919-4962`, either require ≥100 views per row (else emit `(insufficient data)`) or drop the bottom-5 AVOID block entirely and filter non-Shorts rows.
**Why:** at a median of 24 views across 267 videos, the top-5/bottom-5 split is noise injected as instruction into every script prompt. The actual bottom-5 currently includes four 0-view videos plus *"Sale91 Warehouse Live — Office Camera"*, a livestream. The actual top-5 it tells the bot to **mimic** are Ketu's face-led personal founder videos ("Paise Nahi The", "College Ke Baad Direct Business") — a format the pipeline structurally cannot produce, because `:5317-5318` forbids faces and the truth rule forbids inventing personal stories. It is teaching the writer to imitate the one thing it must not do. **Removing a misleading instruction is worth more here than adding a new one.**
**Effort:** small.
**Verify:** print the injected block; it should read `(insufficient data)` today.

---

### 9. Make the blog LD read from `products.json`, and unify the business identity
**Build:**
- `:8104-8123` — fetch `/catalog/products.json` at build time and derive `lowPrice`/`highPrice`. Currently hardcodes `65`/`250`; live range is ₹107–₹450 (₹60 Sale Kids Polo). Verified live on the polo post: the Product LD really does say 65/250.
- `:8108` — `'180-220 GSM, own knitted from Tiruppur'` → 180–430 GSM, Delhi.
- `:8017-8022` — replace the Tiruppur / Tamil Nadu PostalAddress with the real Delhi one.
- Point `organization_ld`, `person_ld.worksFor` (`:8054`) and `article_ld.publisher` (`:8083`) at one shared `@id: "https://www.bulkplaintshirt.com/#business"` — the LocalBusiness node that already holds the Delhi address, geo and hours. Today those three carry three different names ('Sale91.com', 'Own Knitted Blank Wears', 'BulkPlainTshirt.com') with no shared `@id`.
- Cache the fetch; fall back to current literals on 404 so a network blip never breaks a publish.
**Why it drives traffic:** for a query containing the word "Delhi" — his money query — 125 indexed pages tell Google and every AI engine the company is in Tamil Nadu, and quote a price band that undercuts him. Wrong structured data is worse than none: it is the machine-readable claim an engine trusts over prose.
**Effort:** small–medium. The LD builder `:7988-8287` already emits every schema type needed.
**Verify:** `curl` any blog post, pipe through a JSON-LD parser, confirm `addressLocality: "New Delhi"`, `lowPrice: 107`, `highPrice: 450`, and one `@id` shared across all three org references.

---

### 10. Deep-link the blog into the catalog product pages
**Build:** add a rule to the existing internal-link backfill machinery (`.github/workflows/backfill_internal_links.yml`, `repair_internal_301_links.yml`, and the idempotent patch path at `daily_short.py:9778-9824`) that scans each post for GSM tokens and product names, links the first mention to the matching `catalogUrl` from `products.json`, and renders a small "current rate" strip (product · GSM · ₹/pc · MOQ 10 · GST 5% extra) from the same file.
**Why it drives traffic:** the "plain t-shirt wholesale Delhi 240 GSM rate" query is split across two pages that never link to each other. `/p/plain-t-shirt-wholesale-near-me-delhi-india.html` owns the Delhi intent at 2,865 words, has **zero** `<table>`, and the string "240 GSM" does not appear once. `/catalog/p/oversize-240gsm/` has ₹195 in three tables and a 15-question FAQPage — and is a near-orphan ("crawled – not indexed"). An unindexed page cannot be cited by an AI answer no matter how good its markup is. 0 of 6 sampled posts contain a `/catalog/p/*` link.
**Effort:** medium — new rule inside proven plumbing, not new plumbing.
**Verify:** re-sample 10 posts; each must contain ≥1 `/catalog/p/*` link. Re-submit via the existing IndexNow path; check GSC coverage for the catalog pages in 2–3 weeks.

---

### 11. Check the Search Console generative-AI setting, then read the Generative AI performance report
**Build:** (a) two-minute manual check: Search Console → Settings → *Search generative AI*. Rolled out worldwide 2026-08-31; a site must be **included** to be eligible for AI Overviews / AI Mode at all, and child properties **inherit** from the closest parent that stopped inheriting. He has several subdomain properties (live., b2., vlog.) plus a domain property. (b) Add a read leg to the daily run using the GSC API access he already has (`reference_gsc_api_access.md`) and log top AI-impression pages into the existing `health_state.json` / `phase_status.json` surface.
**Why it drives traffic:** if the setting is wrong or inherits an exclude, every other AI-search item on this list is wasted. And the Generative AI performance report is the first real measurement of AI citation — impressions by Pages / Countries / Devices / Dates, with export. It will say whether the catalog pages or the blog posts are the ones surfacing.
**Effort:** small. `:11628` already authenticates to a Google API from the same workflow; `health_watch.py` already exists as the reporting surface. `grep searchconsole|webmasters` → 0 hits.
**Verify:** setting reads "included"; `health_state.json` gains an `ai_impressions` block after one run.

---

### 12. Merge the Delhi page and the 240 GSM page into one page that owns the whole query
**Build:** do not write a third page. `/p/plain-t-shirt-wholesale-near-me-delhi-india.html` already owns Delhi (2,865 words, no rate). `/catalog/p/oversize-240gsm/` already owns the rate (dual AggregateOffer with `eligibleQuantity`, 15-question FAQ). Give the Delhi post the rate table in **plain HTML** (not JS-injected), the Product LD from `products.json`, the LocalBusiness `@id`, an FAQ block answering the literal question in the first sentence, and two-way links.
**Why it drives traffic:** Google's own 2026 guidance says the single biggest lever on AI-search presence is unique, non-commodity content with a first-hand point of view — and names the failure mode as commodity content anyone could have written. He knits the fabric. He can state today's real ₹195, what 240 GSM terry loopknit weighs after biowash, why the XS–XXL band is flat while 260 GSM steps ₹190→₹255 by size, and what a 10-piece mixed MOQ from a Delhi godown means for a printer's cash cycle. Nothing on the site combines city + GSM + number in one indexed page.
**Effort:** medium.
**Guardrail:** do this **once, properly, for the two or three GSMs that carry the business.** Do not template it across every variant — Google explicitly calls that scaled content abuse.
**Verify:** page renders the rate in HTML source (view-source, not devtools); GSC shows it indexed; it appears for "plain tshirt wholesale delhi 240 gsm" within 4 weeks.

---

### 13. Expose `products.json` + llms.txt v2 markdown alternates
**Build:** link `products.json` from `/catalog/`, name it in `llms.txt` under the Pricing heading (which currently says "we don't list prices on this AI index" and then contradictorily prints a full stale price table), and add it to `catalog/sitemap.xml`. Then emit a `.md` twin per catalog page (`page.html.md`), discoverable via `rel="alternate" type="text/markdown"` and `rel="describedby"` — deliverable as a CloudFront `Link:` header without touching any page. `generate-pages.js` already builds every catalog page from structured data, so a `.md` twin is another output target.
**Why:** one file on the whole domain has provably-correct rupee figures, and it is linked from nowhere — 0 references in catalog HTML, 0 in any llms.txt, absent from every sitemap. An agent that finds it answers any rate question correctly and cheaply.
**Effort:** small.
**Verify:** `grep -r products.json /Users/ankit/Projects/catalog/*.html` > 0; `curl -I` a catalog page and see the `Link:` header.
**Caveat to say out loud so he doesn't over-invest:** Google states plainly that Search ignores llms.txt. This is for non-Google engines and agents, and is worth doing only because he's already built 90% of it.

---

### 14. Substitute one of the five Veo clips with real warehouse footage
**Build:** in the clip loop at `:12473`, add a `REAL_BROLL_DIR` keyed to the existing `_detect_series()` `:1640` categories; substitute clip 2 or clip 5 when a topic-appropriate file exists, fall back to Veo otherwise. Gate `containsSyntheticMedia` (`:5651`) on whether any real clip was used.
**Why it drives traffic:** five synthetic clips force `containsSyntheticMedia=True`, which YouTube can surface as a label on a video whose entire purpose is manufacturer credibility. One genuine shot of a real 1-lakh-piece stack or a running knitting machine does more for a bulk buyer's trust than four more AI clips — and it shows actual sellable product.
**Effort:** medium. `tools/reel_pipeline/` is a complete proven encode toolchain for his real footage and is currently disconnected from `daily_short.py` entirely.
**Verify:** one render contains a real clip; the upload's `containsSyntheticMedia` is `False` only when it should be.

---

## PART D — THE SHOT-FORMAT LIBRARY

This is for the **filmed** pipeline (`tools/reel_pipeline/`), not the AI-voice bot. The three spoken-CTA bans at `daily_short.py:5090`/`:5153-5155`/`:5238` apply to the bot reels only — filmed reels may speak a CTA.

### Rule 0 — the shoot-day rate card (makes any ₹ speakable at all)
Before rolling: open `pc.js` on the second phone, read the rate for the product in hand, write it on a card with a marker (card + marker are allowed inventory), and burn a corner date stamp on the render (`शूट: 4 सितम्बर 2026`) — same device already used for perishable Factory Sach episodes. Result: every ₹ he says is a real published rate, satisfying `daily_short.py:5014`. **Never hard-code a rate in a script.**

### Word budget (measured 2.06 words/sec)
15s ≈ 31 words · 18s ≈ 37 · 22s ≈ 45 · 26s ≈ 54 · 30s ≈ 62. **Count in code before shooting** — model-claimed counts run ~10% low.

### On-screen text budget
1. At no instant may burned-in text exceed ~20% of frame area. Current renders are safe: a `cap2.py` caption line is 930×86 px = **3.9%** of 1080×1920; the `build_reel.py` 300 px strip is 15.6% of frame height with text filling maybe half. The risk is *adding a second simultaneous text block*.
2. **One text band at a time.** The only legal overlap is the 0.5s cover.
3. Max 2 lines, ≤5 tokens per line (already the hand-correction rule, README step 2).
4. **Never a text-only frame.** No full-screen mid-reel card. No DM/comment screenshot, no rate-table screenshot — the tee or his hands must be visible behind text in every frame. A rate goes on a handwritten card **held in shot**, not as an overlay.
5. Cover text: 3–6 words TOTAL, two lines split by `|` (HOUSE FORMAT v2, `:1270-1271`).
6. Safe band 25–70% of frame height (`:1317-1318`, verified against both YT Shorts UI and the IG 4:5 grid crop).
7. Colour: white base, black stroke 6px, highlight **#7FD4FF only**, **max ONE highlighted word per line**. Never red/green as a meaning-carrying pair.

**TO VERIFY, NOT ASSUME:** from `cap2.py:36-40` the caption band computes to y=1527..1613, x=75..1005. Instagram's reel player conventionally reserves roughly the bottom 320 px (y>1600) and the right ~140 px (x>940). That puts the caption bottom edge **13 px** into the bottom UI and long lines **65 px** under the action rail. This has never been checked on a real posted reel. **Post one, screenshot it, then decide.** If confirmed: `CY 1570→1500`, `MAX_W 930→820`.

### The 12 formats

| # | Format | 0:00–0:03 exactly | Len / words | Line pattern (Hinglish) | Spoken CTA | Why it converts |
|---|---|---|---|---|---|---|
| **1** | **AAJ KA RATE** (rate card on camera) | Card already in frame filling lower half, hand taps the number once, he is mid-sentence. No hand entering, no greeting. Cover: `240 GSM \| AAJ KA RATE` | 18–22s / ~40 | "[Product] ka aaj ka rate — [₹X] piece. / Ye [GSM] GSM hai, [combed/biowash] hai. / [₹Y] wala isse [one checkable difference] hai. / Rate roz badal sakta hai, isliye date daal di hai. / Bas itna hi." | "Rate check karna ho toh sale91 pe dekh lo." | A B2B buyer is doing arithmetic, not being entertained. His data: spec-number titles median 3,228 views / 0.89% share vs 2,302 / 0.43%. A rate is the one thing a buyer forwards to his partner unedited. **Highest-conversion format.** |
| **2** | **RACK WALK, ONE RATE** | Camera already moving down the rack, hand already pulling a tee. First word on frame 1. Never start static. | 22–28s / ~50 | "Ye poora rack [product] ka hai. / [N] colour, ek hi GSM. / Iska rate [₹X] piece hai — [MOQ] piece se. / Colour change karo, rate wahi rehta hai. / Dekh lo." | "Colour list sale91 pe hai." | Depth-of-stock is the objection a bulk buyer actually has, answered visually. This is the shot that proves manufacturer, not trader. **Clear the aisle of every printed courier bag first.** |
| **3** | **GSM ON THE SCALE** | Two folded tees already on the scale platform, display readable, thumb about to lift one. | 20–25s / ~45 | "Do t-shirt, dono same size. / Ye [A] GSM, ye [B] GSM. / Weight mein [N] gram ka fark hai. / GSM matlab yehi — 1 square meter ka weight. / Aap khud tol ke dekh lo." | "Sample mangwa ke khud tol lena." | Converts an unverifiable adjective ("heavy") into a number the buyer reproduces on his own scale — the "verifiable check" the topic prompt already asks for at `:7443-7451`. |
| **4** | **TUBELIGHT SEE-THROUGH** | Tee already stretched flat against the lit tubelight, light already coming through. Frame 1 is the payoff. | 15–18s / ~33 | "[A] GSM — light aar-paar dikh rahi hai. / Isi liye print through dikhta hai. / Ab [B] GSM dekho — kuch nahi dikhta. / Printing ke liye [B] minimum rakho. / Dhyaan rakhna." | "Dono ka sample ek saath mangwa lo." | The single most-forwarded kind of clip: a one-second visual that settles an argument between a printer and his supplier. Literally the example the script prompt already uses as its model TRUE hook (`:5011`). **This is the demo the burn test should have been.** |
| **5** | **COLLAR STRETCH & RECOVERY** | Collar already at maximum stretch between both thumbs; release at ~0:02 so recovery resolves first. | 18–22s / ~40 | "Collar kheench ke chhodo — wapas aana chahiye. / Ye 1x1 rib hai, elasticity hoti hai. / Flat knit mein ye recover nahi karta. / 5 wash baad collar loose wahin se hota hai. / Sample pe khud kheench ke dekh lo." | "Sample aaye toh pehle collar kheencho." | Collar failure is the #1 post-delivery complaint a reseller eats. **The rib knowledge is already written verbatim at `:5052-5060` — but wrapped in an invented customer. Film the knowledge, drop the customer.** |
| **6** | **BLIND TOUCH TEST** | Both tees face-down on the packing table, marker-written 'A' and 'B' cards on each, hands already rubbing one. | 22–26s / ~48 | "A aur B — dono same colour, same size. / Haath se pata chalega? / Kaan ke paas rub karo — khurdura matlab open end. / Smooth matlab ring spun. / Reveal: A [X] GSM tha, B [Y]. / Try karke dekh lo." | "Comment mein batao aapne kaunsa guess kiya." | The only format that generates **comments**, which feed `get_audience_questions()` `:7392` — currently running on 9 cached comments from the *other* channel. The rub-near-your-ear check is already named as the model verifiable check at `:7444-7445`. |
| **7** | **BUYER QUESTION ANSWERED** | Already answering, mid-sentence, holding the relevant tee. Question appears **only** as a 0.5s cover card. | 25–30s / ~58 | "Sawaal aaya — [question in 6 words]. / Seedha jawab: [one sentence]. / Kyunki [one technical reason]. / Aur agar [common variation], toh [what changes]. / Theek hai." | "Aisa koi sawaal ho toh comment kar do." | The only format that scales without new ideas — every comment is a script. **HARD RULE: never screenshot the DM or comment.** A screenshot puts a buyer's handle in frame (customer data) and makes the reel majority-text, which IG downranks. Read it aloud. |
| **8** | **STOCK COUNT IN PIECES** | Hand already counting down a stack, pieces flicking past, count already in progress. No "aaj hum ginenge". | 15–20s / ~35 | "Ye ek stack — [N] piece. / Is rack pe [M] stack. / Matlab sirf is colour ka [N×M] piece ready. / Order karo toh aaj hi nikal jaata hai. / Bas itna hi." | "Live stock sale91 pe hai." | Answers "ready stock hai ya banwaoge?" — the objection that decides today vs next month. **PIECES, never kilos.** Do not restate the "1 lakh+ ready stock" line from `:106-107` unless verified that day. |
| **9** | **SHOULDER DROP / TAPE** | Tape already laid across the shoulder seam of a flat tee, number already readable. Frame 1 is a measurement. | 20–24s / ~45 | "Same size L — teeno mein shoulder alag hai. / Regular [A] inch, oversized [B], boxy [C]. / Length bhi [D] inch kam hai boxy mein. / Size chart dekhe bina order mat karo. / Naap ke dekh lo." | "Size chart sale91 pe hai — order se pehle dekh lena." | Sizing mismatch is the most common genuine cause of a rejected bulk order, teachable without inventing anyone's rejection. The AI version (29-Aug, 2,139 views / 0.55%) already beat account median share as pure b-roll. |
| **10** | **MACRO DETAIL** (patch / swing tag / stitch) | Extreme close-up, detail already filling the frame, thumbnail already tracing the stitch line. | 15–18s / ~33 | "Ye [detail] dekho — [material/stitch type]. / [Alternative] mein ye [what fails]. / Isliye [durability consequence]. / Blank lete waqt yahi check karo. / Itna kar lo bas." | "Sample lo, yahi jagah check karna." | Macro texture is the only shot type that reads as *manufacturer* rather than *reseller with a warehouse*, and it is the cheapest shot he owns. **HARD RULE: only HIS OWN tag/patch** — a customer's brand patch in frame is customer data. **"Moon patch" appears nowhere in the repo or any memory file — do not script it blind.** |
| **11** | **DISPATCH: COUNT & SEAL** (label-free) | Hands already counting pieces into an open poly bag, count audible. **NO label surface anywhere in frame.** | 18–22s / ~40 | "Har parcel mein pehle count hota hai. / [N] piece, ek-ek karke. / Phir seal, phir weight. / Isi liye short-quantity ki complaint nahi aati. / Bas itna hi." | "Order karoge toh yahi process chalega." | "Kya poora maal aayega?" is a trust question, answered by process footage with no challengeable claim. **The AWB version cannot be shot** — `project_factory_sach_script_pack.md:35-38` bans any label, address, AWB, barcode, or printed courier bag/tape in a parcel frame (courier logo + buyer data). Pre-roll checklist: clear the table of every label, printed bag, and phone screen. |
| **12** | **BURN TEST — 12s cutaway, DEMOTED** | Yarn tip already alight, ash already forming. Macro. | 12s / ~28 | "Cotton jalta hai toh raakh banti hai — haath se bikhar jaati hai. / Polyester pighal ke hard gola banata hai. / Bas yahi fark hai. / Khud karke dekh lena." | "Sample pe ek dhaaga jala ke dekh lo." | **His measured WORST performer** — 11,746 @ 0.75% vs 56,767 @ 2.70% for the zero-prop reel. Also scores Low on new-viewer-return on the main channel. Needs a flame, which is outside allowed inventory. It still earns a slot because it **saves** well even though it does not **forward**. Shoot once, reuse. **Never headline it.** |

### Weekly rotation grid
Mon **1** (rate) · Tue **3 or 9** (measured proof) · Wed **7** (buyer question) · Thu **2 or 8** (scale/depth) · Fri **4/5/6** (in-hand check) · Sat **10** (macro) · Sun **11** (process).
Cap formats **4/5/6/12** (save-heavy demos) at roughly **1 in 6** posts. His data: demos save, rate and process forward, and shares beat saves as a views predictor (Spearman 0.451 vs 0.296).

### The measurement that must ship with the library
Add a `format` key to every entry in `clip_history.json` and `ig_engagement_history.json`. In 6 weeks that gives share-rate **per format** instead of per topic. **This cannot be obtained retroactively** — every existing engagement record is topic-labelled only. `get_top_performing_ig_topics()` `:3712-3718` already ranks by share-rate with a floor; one field makes it rank formats.
**Reading caution:** main-channel numbers are ~90% paid traffic (`project_main_channel_analytics_2026_08_24.md`). Judge formats on Instagram share-rate, or on YouTube only with Traffic source ≠ "YouTube advertising" applied.

---

## PART E — INSTAGRAM WEEKLY WINNER → YOUTUBE PROMOTION

**Verdict on the plan: sound, unusually disciplined, keep it. The one real flaw is the measurement window, not the selection idea.**

### E1. Backfill by enumerating `/{ig-biz}/media` — not by patching the publisher
Do **not** patch `publish_queue.yml` to call `save_ig_upload_record()` — that fixes only future filmed reels and still misses anything posted from his phone. Invert it: walk `/{ig-biz}/media?fields=id,permalink,timestamp,media_product_type,caption` and upsert any REELS not already keyed by `media_id`. That one reader captures bot reels, queue reels, and phone posts permanently.
`tools/crosspost_watch.py:147-166` is already this exact call and can be lifted verbatim. Seed `video_url` from `publish_queue/done/*.json` where `published_media_id` matches; `permalink` from the API for everything else.
**Blocking today:** all 11 filmed reels (founder-story, founder-clip1, fs-ep2-printcrack, fs-ep3-burntest, launch-260gsm, p17-highlight, rakhi-clip1, rakhi-clip2, rakhi-main, rakhi2-train, v18-reel) are absent from `ig_engagement_history.json`. The 117 tracked records are **all** bot/Veo reels. A winner-picker built on the current file would be structurally blind to his only real content. Effort: small.

### E2. Two-pass measurement — judge on day 7, not day 3
`:3668` sets `checked=True` forever; `:3509` hard-skips on it. Median measurement age across his 114 checked records: **73h** (one outlier at 1133h).
Keep the existing 48h/96h pass as `snapshot_d2` — the cover/thumbnail learning loop wants an early clean read. Add `checked_final` at **168h** that re-fetches and writes `views_d7 / reach_d7 / shares_d7 / saves_d7`. **Only `_d7` feeds the winner gate.**
Without this, the picker is a fast-start detector, not a performance detector — it will keep promoting hooky reels and keep missing the slow-compounding explainer reels that are exactly what a B2B buyer saves and sends to a partner. The whole fetch path `:3580-3682` is reusable as-is; you are adding a delay tier, not new API code. Effort: small.

### E3. The gate — shares and saves per REACH, calibrated to his real distribution
Computed from his 112 reels with reach>0:

| metric | p50 | p75 | p90 | p95 |
|---|---|---|---|---|
| share/reach | 0.43% | 0.70% | 1.17% | 1.54% |
| save/reach | 0.58% | 0.87% | 1.26% | — |
| reach | 1,982 | — | 3,878 | — |

~6 reels/week over 22 weeks.

```
reach_d7 >= 2000
AND ( share_rate >= 0.0105
      OR (share_rate >= 0.0070 AND save_rate >= 0.0126) )
rates on REACH, not views.  Hard cap 2/week.  Take top 1 by share_rate.
```
Backtested the primary arm: at 2.5× median share-rate with reach≥1000 it fires on **11 reels across 22 weeks, with 10 of 22 weeks having at least one** — i.e. roughly half the weeks correctly post nothing. (2.0× fires in 14/22 = too loose; 3.0× in 6/22 = too tight.)

**Fix the denominator bug while you're here:** `get_top_performing_ig_topics()` `:3695-3730` ranks by shares/**reach** with a 500-view floor, while `get_ig_engagement_summary()` `:3771-3844` computes `save_rate`/`share_rate` per **views** at `:3788-3793`. Views counts replays, so the denominator inflates on exactly the looping reels that mark a winner. Two functions, two answers. Switch `:3786-3793` to reach.

**Also add** `total_interactions` and `ig_reels_video_view_total_time` to the fetch — both already proven available on his account by the `fs_insights.yml:38` probe. **Probe** `profile_visits`/`follows`/`profile_activity` rather than assuming; the HTTP-400 fallback pattern already exists.
**Do NOT build the gate on website taps or profile visits** — `website_clicks` and `profile_views` exist only account-level at `/{ig-user-id}/insights?period=day`, never per-media. Use them as same-day corroboration only.
**`ig_reels_avg_watch_time` cannot be in the gate yet** — only 16 of 114 records have it (started 12-Aug, median 13,723 ms). Needs ~2 more months. Effort: small.

### E4. The Sunday job — stage, do not publish
There is **no OAuth write credential for the main channel** (`daily_short.yml:106` loads one token, for the bot channel; main is read-only via `CHANNEL_ID_2` `:432`), and per his standing decision the main channel is deliberately manual. **Do not try to auto-publish there.**

Sunday job (`sunday_recap.yml` already runs 19:00 IST with the full env):
1. Pick the winner on `_d7` numbers.
2. Fetch the **clean master** from the recorded `video_url` — the CloudFront `/p/*.mp4` copy. Never a downloaded copy: it carries a burned-in watermark and re-compression that YouTube visibly penalises, and it breaks the no-synthetic-relabel rule. (Verified: `founder-story-r8k.mp4`, `rakhi2-train-r8k.mp4`, `v18-reel-v2-r8k.mp4` all return HTTP 206 on the permanent origin — not the 30-day-purge `files.sale91.com` bucket.)
3. Stage it. Option (a): write `promotion_queue/<date>.json` + a Telegram card with the master URL and ready-to-paste title/description/tags. Option (b): mint a second OAuth token (`generate_token.py:40-44` already requests `youtube.upload`, so it's mechanical) and upload as `privacyStatus: private` with `publishAt` so he only eyeballs and flips.
4. Append the promotion to `crosspost_ledger.json` so `crosspost_watch.py` verifies it went live — that watcher already handles a `youtube` platform with `verify: manual` and nudges once with a direct link.

**CRITICAL:** the promotion path must **not** reuse `upload_to_youtube()` unmodified. `containsSyntheticMedia: True` is hardcoded at `:5651` and would falsely declare his filmed footage synthetic. Gate that flag on record origin (queue-published/filmed = `False`, bot/Veo = `True`). Also strip the bot-channel boilerplate at `:5609-5624` and the unconditional `" #Shorts"` at `:5601-5603`.

**Spacing:** main-channel long-form slot is Saturday evening. Schedule the promoted Short for **Monday or Tuesday** — never the same day, never two promoted Shorts inside 72h even if two winners clear. `publish_queue.yml`'s `publish_at` hold-back is the mechanism, already written. Effort: medium.

### E5. Re-title for search — a hard SECOND gate, not a formatting step
This is where the plan is genuinely weak and it's worth saying plainly. **An Instagram winner wins on shares and loops; a YouTube Short that brings buyers to sale91.com wins on query match.** Different games. A reel that won on IG through a curiosity hook may have zero search demand behind it.

So: **two gates, not one — IG performance AND a real query exists.** The IG caption comes from `get_ig_seo_line()` `:2319` and is hook-shaped. The YouTube title needs the buyer's search string **in front**:
> `240 GSM vs 180 GSM T-Shirt — Farak Kya Hai? | Bulk Plain T-Shirt`

Rank candidate queries against `fetch_source_channel_insights()` `:3851-3950` (already caches the 42k channel's top titles for 24h) so the title borrows demand already proven. **Reject a promotion whose winner maps to no query at all — post nothing that week instead.** `get_topic_tags()` / `get_topic_hashtags()` / `sanitize_tags()` `:5626-5633` already exist. Effort: medium.

### E6. Honest caveats on the plan
1. **Volume.** At 1–2 promotions/week landing in only ~10 of 22 weeks, that's ~10 Shorts per 5 months on main. Slow. Given his stated preference for volume over extreme quality, consider a **softer secondary tier** (share/reach ≥ p75 = 0.70%) that auto-posts to the **bot** channel while only the top tier goes to main. Same picker, two destinations, zero extra judgement calls — and `upload_to_youtube()` already targets the bot channel, so the secondary tier needs no new upload code.
2. "If nothing performed, post nothing" is the **correct** default and his data supports it — at a sensible threshold roughly half his weeks genuinely have no winner.
3. Shorts on the 42k main channel feed the same subscriber base and are fine — keep them off Saturday and cap at one per 72h so the Shorts shelf doesn't crowd the long-form that actually converts.

---

## PART F — PER-CALL-SITE MODEL RECOMMENDATIONS

### F0. Pre-flight — I already ran the migration audit. Results:
- `temperature` / `top_p` / `top_k`: **0 hits.** ✅
- Assistant-turn prefills (`"role": "assistant"`): **0 hits.** ✅
- `budget_tokens`: **0 hits.** ✅
- `output_format` at `:8719` is an **image-generation** param, not Anthropic. ✅
- `cache_control`: **0 hits** — no caching anywhere.

So the migration to Opus 5 causes **no 400 errors**. The entire hazard is `max_tokens`.

### F1. THE ONE THING THAT WILL BREAK THE PIPELINE IF YOU IGNORE IT
On **Claude Opus 5, thinking is ON by default** (unlike Opus 4.6, where omitting `thinking` meant no thinking), and **`max_tokens` caps thinking PLUS response text together.** Six sites run `max_tokens ≤ 800` and one runs **120**. A naive model-string swap makes them return empty or truncated **with no exception raised**.

`blog_cluster_collision()` `:8404` (`max_tokens=120`) **fails open** at `:8388-8390` — a dead duplicate-detector fails *silently* and starts publishing near-duplicate blog posts. That is the worst failure mode in the file.

Same trap on Sonnet 5 for the blog: adaptive thinking is on by default **and** Sonnet 5 uses a new tokenizer producing ~30% more tokens for the same text. `generate_blog_post()` at `max_tokens=16000` will truncate a full-length article.

**Every model-string change must ship in the same commit as its `max_tokens` change. Dry-run one full pipeline before the cron picks it up** (`feedback_dry_run_before_deploy.md`).

### F2. The table

| # | Site | Now | → Model | max_tokens | effort | Rationale |
|---|---|---|---|---|---|---|
| 1 | `refresh_thumbnail_research` `:1126` | opus-4-6 / 800 | `claude-sonnet-5` | **4000** | `low` | Weekly, 7d cache. Pattern summarisation. Sonnet 5 is cheaper + better than sonnet-4-6. **Also: strip or ground the "analyze top-performing Reels" instruction — see F4.** |
| 2 | `generate_thumbnail_brief` `:1360` | opus-4-6 / 800 | `claude-opus-5` | **4000** | `high` | Vision + it picks the text colour and he is R-G colour blind. Opus 5 vision is 2576 px vs 1568 px on 4.6 — meaningfully better at reading a Veo frame. **Also needs the fabrication sweep (queue item 4).** |
| 3 | `generate_ig_carousel_draft` `:3090` | haiku-4-5 / 900 | **keep** `claude-haiku-4-5` | 900 ✅ | n/a | Haiku 4.5 has no adaptive thinking, so 900 stays safe. Only fix the cost bucket (F3). |
| 4 | `search_trending_topics` `:7504` | opus-4-6 / 800 | `claude-opus-5` | **4000** | `high` | Generates the day's subject. Top tier justified. **Also F4.** |
| 5 | `review_topic` `:7584` | opus-4-6 / 200 | `claude-haiku-4-5` | 200 ✅ | n/a | Outputs a 0–40 int plus one sentence, fires **5×/run**. Haiku has no adaptive thinking → the 200 cap needs no change. Biggest latency win in the file. |
| 6 | `smart_pick_topic` fallback `:7690` | opus-4-6 / 200 | `claude-haiku-4-5` | 200 ✅ | n/a | Same shape, rare. |
| 7 | `review_script` `:7775` | opus-4-6 / 300 | `claude-opus-5` | **3000** | `high` | **This is the fabrication guardrail** — the thing the 60-post incident argues for spending on. Keep top tier. Ships together with the rubric rewrite (queue item 3). |
| 8 | `optimize_title` `:7904` | opus-4-6 / 400 | `claude-opus-5` | **3000** | `medium` | Three platform-specific titles with genuinely different SEO logic incl. the Latin-script-only blog rule at `:7897-7899`. Ships with queue item 4. |
| 9 | `blog_cluster_collision` `:8404` | opus-4-6 / **120** | `claude-haiku-4-5` | 120 ✅ | n/a | Answers with one slug or the literal string `NONE`. **Moving this to Opus 5 at 120 would silently kill it and it fails open.** Haiku is both cheaper and safer. |
| 10 | `generate_blog_post` `:8908` | sonnet-4-6 / 16000 | `claude-sonnet-5` | **32000 + stream** | `medium` | Cheaper *and* better. But adaptive thinking is on by default AND the new tokenizer is ~30% heavier — 16000 will truncate. Must stream at that size (`client.messages.stream()` → `.get_final_message()`). |
| 11 | `generate_reddit_post` `:9741` | haiku-4-5 / 1500 | **keep** `claude-haiku-4-5` | 1500 ✅ | n/a | Fine as is. Fix cost bucket only. |
| 12 | `main()` script writer `:12085` | opus-4-6 / 2500 | `claude-opus-5` | **8000** | `high` | Writes what he actually films and enforces the courier/incident/rupee/pieces constraint stack. **At 2500 with thinking on, the JSON would truncate on most runs and the recovery path at `:12106-12115` would fire constantly.** |
| 13 | `health_watch.py:211` probe | haiku / 1 | **keep** `claude-haiku-4-5` | 1 ✅ | n/a | `max_tokens=1` is correct *because* Haiku has no adaptive thinking. Do not move this to Opus 5. |
| 14 | `.github/qa_full_loop.py:69` | `claude-sonnet-4-5` | `claude-sonnet-5` | per invocation | — | Manual harness, stale pin. |

Model IDs are complete as written — **never append date suffixes.** Replace `claude-haiku-4-5-20251001` at `:3090`, `:9741`, `health_watch.py:211` with the alias `claude-haiku-4-5`.

### F3. Fix the cost numbers — every future decision rests on them
- `daily_short.py:2599-2600` — opus `0.015`/`0.075` → **`0.005`/`0.025`**. `claude-opus-4-6` is $5/$25, not $15/$75. Every opus figure in `cost_tracker.json` is exactly **3× too high**: recorded $46.77 is really ~$15.59.
- Add `claude_haiku_input_1k: 0.001` / `claude_haiku_output_1k: 0.005` and a third branch in `track_claude_call` `:2629-2638` (currently a two-way opus/else split). `:3102` and `:9759` both pass `"sonnet"` for **Haiku** calls — another exact 3× over-count.
- **Net: true Claude spend on tracked sites is ~$4.70/mo, not the ~$11.06/mo reported.** The $10 daily circuit breaker at `:420`/`:12041` is being fed inflated figures and can trip early on a day Veo genuinely needed the headroom.
- **Instrument the 7 untracked Opus sites** (1, 4, 5, 6, 7, 8, 9). `review_topic` fires 5×/run, `review_script` once per attempt — ~$0.08/run (~$2/mo, roughly 40% of true Claude spend) is invisible. `cost_tracker`/`claude_client` are already threaded into every one of these as parameters and `resp.usage.input_tokens`/`.output_tokens` are already available. One line copied from `:12088` each.

### F4. The fabrication surface hiding inside two model calls
`search_trending_topics()` `:7504` and `refresh_thumbnail_research()` `:1126` both ask Claude to report what is "performing well on Instagram Reels AND YouTube Shorts" **with no web-search tool declared** — pure training recall presented as research. That is the same fabrication surface as the 60-post incident. The IG-carousel and cover-outcome context blocks feeding site 1 at `:1090-1120` **are** real data; the "analyze top-performing Reels in the Indian business niche" instruction is not.
Fix: either delete that instruction and rely only on his own measured data, or declare `{"type": "web_search_20260209", "name": "web_search"}` so the claim is grounded. Deleting is cheaper and probably better.

### F5. Prompt caching — now actually possible
`get_script_prompt()` renders a 22,929-char template (~5.7k static tokens) re-sent in full on **every** one of up to 5 retries. `get_blog_prompt()` is 15,227 chars (~3.8k). Opus 4.6's minimum cacheable prefix is **4096 tokens**, so caching could barely fire; **Opus 5 drops it to 512**, and Sonnet 5 to 1024. After the migration, put `cache_control: {"type": "ephemeral"}` on the last static block of both prompts (the volatile per-day context must come **after** it — render order is `tools` → `system` → `messages`, and any byte change invalidates everything downstream).
Money saved: small. Latency saved on a 5-attempt retry loop: real. Verify with `response.usage.cache_read_input_tokens` > 0 on attempt 2.

---

## PART G — WHAT NOT TO DO, AND WHY

1. **Do not switch to GPT-6 Astra or any non-Claude provider.** Grounded in the real ledger: Claude is ~$6.80/mo true out of a $136/mo pipeline; Veo is $119/mo (87.5%). Astra at $10/$50 is 2× Opus 5's $5/$25 — moving the reasoning tier takes ~$4/mo to ~$8/mo, against rewriting 12 call sites, swapping the SDK, re-shaping the base64 vision content-block at `:1360` (Anthropic format), and re-tuning Hindi/Hinglish prompts that 178 production runs have calibrated. GPT-5.6 Luna genuinely is 5× cheaper than Haiku — but the Haiku work is $0.40/mo, so an 80% saving is **$0.32/month** for a second SDK dependency. The only defensible non-Claude case is disagreement diversity on `review_script()` `:7775`, and that is a quality argument, not a cost one.

2. **Do not chase the model-tiering savings as if they were money.** The whole per-site exercise in F2 is worth about **$1.20/month**. Say that out loud rather than dressing it up. The value is latency, correctness, and not silently killing `blog_cluster_collision`.

3. **Do not use the Batch API.** `generate_reddit_post`, `generate_ig_carousel_draft`, `refresh_thumbnail_research` are all eligible for 50% off. The discount is worth **~$0.20/month.** Recorded here so it doesn't get re-proposed.

4. **Do not headline the burn test.** 11,746 views @ 0.75% shares vs 56,767 @ 2.70% for the zero-prop reel. Heaviest props, worst result, Low new-viewer-return on the main channel, and it needs a flame that isn't in allowed inventory. It is a 12s cutaway, shot once, reused.

5. **Do not shoot "dispatch with AWB on camera."** `project_factory_sach_script_pack.md:35-38` is a standing rule: no label, address, AWB, barcode, or printed courier bag/tape in any parcel frame — courier logo breaks the no-courier rule (only Porter is nameable) and the address breaks no-customer-data. Format 11 is the label-free replacement.

6. **Do not screenshot a DM, comment, or rate table into a reel.** Buyer handle in frame = customer data, and a majority-text frame is exactly what Instagram downranks.

7. **Do not re-raise Reddit backlinks.** Settled 01-Aug: all outbound links nofollow, API posting banned, 5 of 7 target subs forbid it.

8. **Do not hard-code any ₹ figure into a prompt, a memory file, or a shot script.** Standing rule after four repeats (`project_dk2_wrong_nonanswer_audit.md:25`): any ₹ in a prompt is *history* and drifts. Rates come off live `pc.js` at shoot time.

9. **Do not "fix" the yellow highlights.** `build_reel.py:31`, `cover.py:14`, `daily_short.py:251` and `COVER_HL_YELLOW :367` are yellow, which is CVD-safe and deliberate. Only `daily_short.py:13226` (red-orange) and the `"Red (#FF0000)"` recommendation at `:1315` are violations.

10. **Do not patch `publish_queue.yml` to call `save_ig_upload_record()`.** It fixes only future filmed reels and still misses phone-posted ones. Enumerate `/{ig-biz}/media` instead (E1).

11. **Do not build the winner gate on website taps or profile visits.** They are account-level only (`/{ig-user-id}/insights?period=day`), never per-media. Same-day corroboration at most.

12. **Do not put `ig_reels_avg_watch_time` in the gate.** 16 of 114 records have it. Revisit in ~2 months.

13. **Do not re-upload a downloaded copy of an IG reel to YouTube.** Burned-in watermark + re-compression, visibly penalised, and it breaks the no-synthetic-relabel rule. Use the CloudFront `/p/*.mp4` master.

14. **Do not reuse `upload_to_youtube()` unmodified for filmed promotions.** `containsSyntheticMedia: True` at `:5651` would falsely declare his real warehouse footage synthetic.

15. **Do not template the Delhi + GSM + rate page across every variant.** Once, properly, for the two or three GSMs that carry the business. Google explicitly calls the spun version scaled content abuse.

16. **Do not over-invest in llms.txt v2.** Google states plainly that Search ignores llms.txt and it will "neither harm nor help" rankings there. It is worth doing only because the marginal cost is near zero and 90% is already built. The *stale root file* (queue item 2) is a different matter — that one is actively costing him money by quoting rates below his card.

17. **Do not swap a model string without changing `max_tokens` in the same commit.** See F1. `blog_cluster_collision` at 120 fails open.

---

### First session, in order
1. UTM (item 1) — 20 min.
2. `COST_RATES` + haiku branch + 7 instrumentation lines (F3) — 30 min.
3. `review_script` rubric + TRUTH dim + third `_fabricated_incident_sentences()` call site (item 3) — 1h.
4. Title branches + thumbnail-brief sweep + delete the 4 surviving examples (item 4) — 1h.
5. Clip-dedup truncation (item 5) — 20 min.
6. CTA blue (item 7) — 1 min.
7. Root `llms.txt` deploy (item 2) — separate repo, 30 min + cache wait.

Then dry-run one full pipeline before touching any model string.